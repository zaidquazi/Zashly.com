[build]
  command = "npm --prefix frontend run build"
  publish = "frontend/dist"

[build.environment]
  NODE_VERSION = "18"

# Single Page App redirect so client-side routes work on Netlify
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
